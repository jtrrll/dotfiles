---
name: linux-ci

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "*" ]
  workflow_dispatch:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        run: sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply jtrrll
...
